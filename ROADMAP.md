# ROADMAP

이 저장소의 프로젝트들을 **어느 방향으로, 왜 그 순서로** 키울지 적는다.
발견한 ici 결함은 [ICI-GAPS.md](ICI-GAPS.md) 에, 프로젝트 소개와 구조 규칙은
[README.md](README.md) 에 있다.

## 배경

이 프로젝트들은 재미와 검증을 동시에 노리고 시작했다. 실제로 쓸 C++/Qt 앱을 만들고,
그걸 [ici](https://github.com/jihoon22-lee/ici) 로 검증한다. ici 는 그 전까지 **자기 자신
(순수 Python 프로젝트) 하나만** 도그푸딩하고 있어서 C++ 경로에 E2E 회귀 커버리지가 없었다.

결과는 기록이 말한다. 세 프로젝트를 만드는 동안 **ici 결함 17건을 찾았고 그중 13건이 수정됐다.**
전부 만들면서 나왔고, 코드를 읽어서 찾은 것은 하나도 없다.

현재 상태 (ici v0.5.5 기준, 전부 우회 설정 없이):

| 프로젝트 | 검증 | 비고 |
|---|---|---|
| `diskmap` | PASS · TEM 4.86 | 코어 + Qt6 GUI |
| `loglens` | PASS · TEM 4.94 | 코어 + Qt6 GUI |
| `ici/viewer` | PASS · TEM 4.94 | ici 저장소에 내재화 |

## 문제: 세 프로젝트가 전부 어댑터를 피해간다

남은 ici 결함 중 무거운 둘은 **A-2**(루트 빌드 디스크립터 거부)와 **A-3**(테스트 컴파일이
plain g++ 고정 — moc 없음, 테스트 프레임워크 없음)이다. 이 둘이 Phase 5(Qt/CMake 어댑터)의 본체다.

그런데 이 결함들이 아직 실측되지 않은 이유는 간단하다. **정면으로 부딪히는 대신 우회했기 때문이다.**

- 로직을 전부 Qt 없는 `core` 로 밀어냈다 → **moc 가 필요한 테스트가 하나도 없다**
- `CMakeLists.txt` 를 하위 디렉터리에 두었다 → build 엔진의 루트 거부에 안 걸린다
- 자체 ASSERT 매크로를 썼다 → 테스트 프레임워크 지원이 아쉬울 일이 없다

우회 자체는 당시 옳은 선택이었다. 하지만 지금 이 상태로 Phase 5 를 설계하면 **실측이 아니라
그 우회에서 역추론한 요구사항**으로 설계하게 된다. 원래 계획에 적어둔 문장이 그대로 유효하다 —
*어댑터는 실물 Qt 프로젝트를 대상으로 설계해야 요구사항이 추측이 아니라 실측이 된다.*

**그래서 키우는 방향은 "우회를 깨는 쪽"이다.** 기능을 늘리는 것 자체가 목적이 아니다.

## 방향과 그것이 실측하는 것

| # | 방향 | 깨는 우회 | 실측되는 ici 요구사항 |
|---|---|---|---|
| ③ | loglens **라이브 팔로우** | 로직을 전부 core 로 밀어냄 | **moc** — 시그널/슬롯이 얽힌 모델을 단위 테스트하려면 필요 |
| ① | diskmap **정리 작업대** | 〃 | **moc** — undo 스택·스테이징 모델 |
| A | **diff/merge 도구** (신규) | 〃 + 아이콘·폼 | **moc** + `.qrc`/`.ui` → **CMake 생성 단계** |
| ④ | loglens **하이라이트 룰 편집기** | 〃 | moc + 편집 모델 검증 |
| ② | diskmap **스냅샷 비교** | (없음 — core 알고리즘) | 없음. 순수하게 도구 가치 |

## 순서와 근거

### 1단계 — loglens 라이브 팔로우 (③)

**`loglens` 인데 `tail -f` 가 안 된다.** `FileTailer` 는 core 에 있는데 GUI 가 한 번만
`poll()` 하고 만다. 가장 명백한 구멍이고 가장 작다.

겉보기와 달리 까다로운 부분이 모델에 있다.

- 필터가 걸린 상태에서 새 줄이 들어올 때 `beginInsertRows` 범위 계산
- 자동 스크롤을 언제 멈출지 — 사용자가 위로 스크롤했으면 따라가면 안 된다
- 로테이션·truncate 시 모델 리셋

이건 `QAbstractTableModel` 서브클래스에 있어야 하고, **인덱스 매핑이라 테스트 가치가 높다.**
그래서 "moc 없이 이 테스트를 링크할 수 없다" 는 상황을 처음으로 정면에서 만나게 된다.

**완료 기준**: 팔로우 모드가 동작하고, 그 모델 로직을 단위 테스트하려 할 때 ici 가 어디서
막히는지 `ICI-GAPS.md` 에 기록됐다.

### 2단계 — diskmap 정리 작업대 (①)

디스크 사용량을 보는 이유는 대개 **지우려고** 인데, 지금은 보고 끝이다.

다중 선택으로 삭제 후보를 담고, 회수 용량을 보여주고, 실행 전 검토한다. 핵심은 **휴지통으로
이동**이라 되돌릴 수 있다는 점이다.

- 순수 core: 회수 용량 계산, 중첩 선택 정규화(부모를 고르면 자식은 빠진다), 안전 규칙
  (심볼릭 링크·시스템 경로 거부)
- Q_OBJECT: 스테이징 모델 + `QUndoCommand` 스택 — 여기 말고 둘 데가 없다

**완료 기준**: 스테이징·실행·되돌리기가 동작하고, undo 스택의 단위 테스트를 시도한 결과가
기록됐다. 1단계와 같은 벽이면 그 사실이 요구사항의 재현성을 뒷받침한다.

### 3단계 — diff/merge 도구 (A, 신규 프로젝트)

앞의 둘로 moc 요구사항이 실측된 뒤이므로, 여기서는 **처음부터 Qt 중심으로** 설계한다.

- 순수 core: **Myers diff** — 알고리즘이라 단위 테스트가 아름답게 나온다
- Q_OBJECT: 헝크 모델(충돌 판정·accept/reject 상태·결과 산출), 해결 undo,
  `QSyntaxHighlighter`
- **`.qrc`(아이콘) / `.ui`(폼)** 를 의도적으로 넣어 **CMake 생성 단계**를 요구사항으로 만든다

병합 해결 상태는 본질적으로 모델에 살아야 한다 — 억지로 옮기는 게 아니다. 도메인도 또 다르다:
`diskmap`(파일시스템·기하), `loglens`(텍스트 파싱·술어) 에 이어 **시퀀스 정렬 + 편집 상태 관리**.

**완료 기준**: A-2(루트 빌드 디스크립터)와 A-3(moc·프레임워크) 양쪽이 구체적 재현 조건과 함께
`ICI-GAPS.md` 에 기록됐다.

### 4단계 — 여유가 되면

- loglens 하이라이트 룰 편집기 (④) — 룰이 코드에만 있다. UI 로 정의·저장·드래그 재정렬
- diskmap 스냅샷 비교 (②) — "지난주 대비 뭐가 늘었나". 트리 diff 는 좋은 core 소재

## Phase 5 로 넘어가는 조건

3단계까지 마치면 `ICI-GAPS.md` 에 **A-2·A-3 의 재현 조건이 실물로** 쌓인다. 그때
[ici 의 `docs/design/`](https://github.com/jihoon22-lee/ici/tree/main/docs/design) 에 Qt/CMake
어댑터 스펙을 쓴다. 그 저장소의 `ci-validation-roadmap.md` 에 build adapter 와 compile DB 가
이미 **보류 항목으로 적혀 있으므로**, 새 문서를 만들기보다 그것을 이어 쓰는 편이 맞다.

## 원칙

**완료 기준을 "기능이 동작한다" 가 아니라 "요구사항이 실측됐다" 로 둔다.** 기능은 수단이다.
각 단계는 PR 하나 크기로 끊고, 매번 ici 가 어디서 어떻게 깨지는지를 재현 조건과 함께 남긴다.

지금까지 찾은 결함 13건이 전부 그렇게 나왔다 — 만들다 이상해서 파고든 결과다.
