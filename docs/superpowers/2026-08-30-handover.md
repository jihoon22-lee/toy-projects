# 인수인계 — toy-projects (2026-08-31 업데이트)

다른 세션이 이어받아 작업할 수 있도록 쓴 문서다. **저장소를 읽으면 알 수 있는 것은 적지 않는다.**
여기 있는 건 코드와 커밋 로그에 남지 않는 것들 — 왜 그렇게 정했는지, 무엇에 물렸는지다.

`ici` 저장소 쪽 인수인계서가
[`ici/docs/superpowers/2026-08-30-handover.md`](https://github.com/jihoon22-lee/ici/blob/main/docs/superpowers/2026-08-30-handover.md)
에 있다. ici 엔진 내부 이야기는 그쪽에 더 자세하다.

## 1. 지금 어디까지 왔나

**끝난 것.** `loglens` 는 CMake, `diskmap` 은 qmake 로 전환했다
([#10](https://github.com/jihoon22-lee/toy-projects/pull/10)). GUI 헤더는
`include/<project>/gui/` 로 옮겼다([#11](https://github.com/jihoon22-lee/toy-projects/pull/11)).
`loglens` 에 라이브 팔로우가 생겼고, 두 프로젝트 모두 `Q_OBJECT` 단위 테스트가 `tests/` 안에서
통과한다 — `loglens` 는 `QAbstractItemModelTester`, `diskmap` 은 `QSignalSpy`.
제품 우선순위와 ici 교차 검증 순서를 정한 마스터 계획은
[#12](https://github.com/jihoon22-lee/toy-projects/pull/12)로 병합됐고, loglens의 poll 경계
상태 보존은 [#14](https://github.com/jihoon22-lee/toy-projects/pull/14)로 병합됐다. file
identity와 typed source 결과는 [#20](https://github.com/jihoon22-lee/toy-projects/pull/20)으로
병합됐으며, 현재 `feat/loglens-follow-recovery` 브랜치에는 L1 Slice 2 follow recovery 구현
커밋 `4094ff5`, 회귀 테스트 커밋 `ca56d2e`, inode 재사용 복구 보완 커밋 `d419d2f`가
있다.

| 프로젝트 | 빌드 | 검증 | 테스트 |
|---|---|---|---|
| `loglens` | CMake 어댑터 | PASS · TEM 4.84 | 10 |
| `diskmap` | qmake 어댑터 | PASS · TEM 4.85 | 7 |

ici 0.6.0 이 필요하다. `.github/workflows/ci.yml` 의 `ICI_VERSION` 이 `v0.6.0` 으로 고정돼 있고,
CI 는 릴리스 에셋을 체크섬 검증해 내려받는다(소스 빌드가 아니다).

**현재 기준.** 마스터 계획의 T0-1부터 T0-5까지 구현과 로컬 native/ici 실측은 완료됐다.
T0-5의 `chore/qt5-qt6-matrix`는 discovery에서 각 GUI project를 Qt5·Qt6으로 확장하고,
선택 toolchain·pkg-config·native tests·headless smoke를 확인한다. T0-5의 완료 증거에는
GitHub Actions 네 matrix leg와 Merge Gate의 성공도 포함한다. 이후 L/D/B/E/A/Q stream의
완료 여부는 마스터 계획 체크리스트와 각 PR의 native/ici 실측으로 판단한다.

L1 Slice 2는 Qt 6과 Qt 5 CMake/CTest에서 `test_main_window`를 포함한 10/10 native PASS까지
확인했다. retryable source 오류 중에는 기존 행과 Follow를 유지하고, replacement 재등장 시
새 generation으로 복구하며, 사용자의 중지/재개와 fatal unsupported source를 구분한다.
이 브랜치에서 `/home/jihoon/projects/ici/dist/ici.pyz` version 0.6.0으로 실행한 local
verify도 `Suite PASS`, 10 pass/0 warn/0 fail/0 error/2 skip, 10/10 tests, line 92.5%,
function 97.1%, branch 80.8%, TEM 4.86, maximum complexity 12, duplication 1.3%, elapsed
81.28초를 기록했다. HTML `/tmp/loglens-recovery-fixed.html`은 265789 bytes이며 external
HTTP `src`/`href` references는 0개였다. Qt5와 Qt6 offscreen GUI smoke도 각각 8초 timeout
전체 동안 생존했고, 예상 종료 코드 124를 확인했다. 원격 GitHub Actions, report-pr sticky
댓글과 Pages 링크 검증은 아직 PR 게이트로 남아 있다.

PR #22의 첫 CI 실행
[`33335607699`](https://github.com/jihoon22-lee/toy-projects/actions/runs/33335607699)은
missing 직후 device/inode이 재사용될 수 있는 환경에서
`disablingFollowWhileWaitingStopsPolling`을 Qt5·Qt6 native와 `ici verify` 모두
실패시켰다(rowCount 2). 더 큰 replacement가 기존 offset에서 읽혀 stale/new bytes가
섞인 것이 원인이었다. `d419d2f`는 `recovery_pending_`/
`recovery_restart_started_`로 unavailable interval 뒤 identity와 무관하게 단 한 번
새 generation을 시작하고 offset을 reset하도록 보완했다. 수정 후 Qt5·Qt6 전체 CTest는
다시 10/10 PASS다. 이 커밋을 포함한 원격 CI 재실행, sticky comment HTML 게시와 Pages
HTTP 확인은 아직 pending이다.

## 2. 다음에 할 일

장기 작업의 기준은
[`제품 포트폴리오와 ici 실물 검증 마스터 계획`](plans/2026-08-30-product-portfolio-master-plan.md)이다.
기존 [`Qt 셸 세부 계획`](plans/2026-08-30-qt-shell-tests.md)은 T0의 입력과 근거로 보존하되
작업 순서와 완료 조건을 결정하지 않는다. 마스터 계획이 우선하며, stateless `parseLines` 대신
poll 경계를 보존하는 stateful assembler를 먼저 설계한다.

완료된 T0-2의 `RecordAssembler`가 line/continuation/partial state를 poll 경계에 보존하고,
T0-3~T0-5가 두 Qt 셸과 version matrix를 닫았다. L1 Slice 1과 Slice 2의 구현/native 검증이
끝났으므로, 다음 순서는 PR #22에 d419d2f를 포함한 원격 CI 재실행과 report-pr sticky
HTML/Pages 증거를 확보하고 머지한 뒤 diskmap D1 Slice 2의 cycle-safe traversal·hardlink
aggregate·path abstraction이다.
`bucketTotal` 같은 이전 세부 계획의 이름은 마스터 계획의 현재 계약에 없는 한 별도 작업으로
부활시키지 않는다.

ici 쪽 기준은
[`Python·C++·Qt 품질 분석기 마스터 계획`](https://github.com/jihoon22-lee/ici/blob/main/docs/superpowers/plans/2026-08-30-python-cpp-qt-quality-analyzer-master-plan.md)이다.
T0의 Qt 셸 작업은 독립적으로 진행할 수 있지만 이후 buildscope/compile context,
envlens/Python compatibility, abilens/Make·ABI는 마스터의 교차 저장소 순서를 따른다.

diskmap 정리 작업대는 즉시 delete UI부터 만들지 않는다. identity-safe node, hardlink/symlink,
scan generation과 실행 직전 재검증을 먼저 구현한 뒤 recoverable trash workflow로 이어간다.

## 3. 결정과 그 이유 — 다시 논의하지 말 것

**"로직은 core, Qt 는 얇은 껍데기" 원칙은 폐기됐다.** 사용자 판단이다 — Qt 는 GUI 툴킷이기
전에 프레임워크이고, 내부 개발에서는 Qt 5.15 로 core 까지 구현한다. **`README.md` 에서 그
문장을 제거하고, CLI와 GUI가 같은 핵심 의미론을 공유한다는 현재 규칙으로 정리했다. Qt를
core에서 금지하지 않는다는 원칙과 Qt 5/Qt 6의 실제 지원 완료는 별개였으며, T0-5에서
양쪽 build/test/smoke 증거를 확보해 완료로 표시했다.

계획이 `parseLines` 를 core 로 옮기는 이유는 원칙 때문이 아니라 **그것이 파서 로직이고 테스트
대상이기 때문**이다. 지금은 익명 네임스페이스라 테스트가 이름을 부를 수 없다.

**빌드 시스템이 프로젝트마다 다른 것은 의도다.** ici 에 어댑터가 둘인데 실물 프로젝트가
하나뿐이면 나머지 하나는 픽스처만으로 설계한 것이 된다. 실제로 `diskmap` 을 qmake 로 옮기고
나서야 드러난 ici 결함이 둘 있었다(§5).

**헤더는 `include/<project>/gui/`.** `include/gui/` 가 아니라 접두사를 유지한 이유는 그러지
않으면 트리에서 유일하게 접두사 없는 경로가 되어 규칙이 다시 둘이 되기 때문이다.

**`loglens` 의 커버리지 임계값은 branch 75/function 92로 상향됐다.** GUI 셸을 포함한 ici
0.6.0 실측이 line 93.2%, function 96.9%, branch 81.8%, TEM 4.84였고, 남은 QPainter/Qt
내부 예외 경로에 합리적 slack을 둔 값이다. `loglens/ici.toml` 에 파일별 수치와 근거가 있다.
`diskmap` 은 셸이 작아 80/90 을 그대로 두고도 통과한다. **수치에 맞춰 임계값을 낮추지 않고
검증 가능한 application state를 우선 게이트한다.**

## 4. 물릴 자리

**빌드 정의에 GUI 헤더를 명시적으로 나열해야 한다.** CMake `AUTOMOC` 은 `.cpp` 와 같은
디렉터리에 같은 이름인 헤더만 자동으로 찾고, qmake 는 `HEADERS` 에 적힌 것만 moc 에 넘긴다.
헤더가 `include/` 로 간 뒤로는 둘 다 자동 탐지가 끊긴다. **안 적으면 `Q_OBJECT` 클래스가
조용히 vtable 미해결로 링크 실패한다** — 어댑터로 없앤 바로 그 실패다. 위젯을 추가할 때마다
이 자리다.

**`ici verify` 가 도는 중에 그 프로젝트 파일을 편집하지 마라.** 한 번 그래서 링커 에러를
레이스로 오진했다.

**`sudo` 가 암호를 요구한다.** 패키지 설치는 사용자에게 부탁해야 한다.

## 5. 픽스처가 놓친 결함 — 이 저장소가 존재하는 이유

`diskmap` 을 qmake 로 옮기면서 ici 결함 둘이 드러났다. **ici 의 qmake 픽스처는 둘 다 통과한
상태였다.**

- **qmake 는 Qt 링크 테스트를 `target_wrapper.sh` 로 실행한다.** ici 가 그 줄을 못 읽어
  테스트 6개 중 5개만 셌다. 픽스처는 Qt 테스트가 하나뿐이라 XML 폴백이 구해줬다.
- **qmake 는 상대 경로로 컴파일한다.** `.gcov` 파일명이 `^#^#^#src#format.cpp` 가 되고 line
  커버리지가 통째로 유실됐다. **픽스처는 그 코드 경로를 실제로 지나갔는데도** 커버리지 존재
  여부만 단언하고 값을 안 봐서 통과했다.

두 번째가 교훈이다. **픽스처를 만들 때 "지나가는가" 가 아니라 "무엇을 단언하는가" 를 봐야 한다.**

전체 목록은 [`ICI-GAPS.md`](../../ICI-GAPS.md) 의 "어댑터 작업에서 새로 발견한 것" 에 있다.

## 6. 환경

WSL2 + WSLg 라 **GUI 앱이 Windows 데스크톱에 실제 창으로 뜬다.** 헤드리스로 돌릴 때만
`QT_QPA_PLATFORM=offscreen` 을 준다. `diskmap` 의 위젯 테스트는 `QWidget` 을 만들므로 검증할
때 이 변수가 필요하다.

| 도구 | 버전 | 비고 |
|---|---|---|
| cmake / ctest | 4.2.3 | |
| qmake6 | 6.10.2 | `/usr/bin/qmake6`, Qt 6 |
| Qt6 Core/Widgets/Concurrent/Test | 6.10.2 | pkg-config로 확인 |
| qmake / Qt5 Core/Widgets/Concurrent/Test | 5.15.18 | `/usr/bin/qmake`, 현재 설치됨 |

내부망은 Qt 5.15 다. **Qt 5 와 어긋나는 소스는 두 줄뿐이다** —
`diskmap/src/gui/treemap_widget.cpp` 의 `event->position()`(Qt 6 전용, Qt 5 는 `localPos()`).
나머지는 이미 양쪽에서 컴파일된다. T0-5에서 CMake와 qmake 양쪽 major를 실제로 빌드했다.

실측 명령은 `qmake -v`, `qmake6 -v`,
`pkg-config --modversion Qt5Core Qt5Widgets Qt5Concurrent Qt5Test Qt6Core Qt6Widgets Qt6Concurrent Qt6Test`다.
2026-08-31 결과는 각각 Qt 5.15.18과 Qt 6.10.2였고, 네 모듈 모두 해석됐다. ici 0.6.0
검증은 `QT_QPA_PLATFORM=offscreen ../../ici/dist/ici.pyz verify --report --html
verify_report.html --github-summary`로 수행했으며 loglens는 `Suite PASS, TEM 4.84
(10 passed, 2 skipped)`, diskmap은 `Suite PASS, TEM 4.85 (7 passed, 2 skipped)`였다.
Qt 5 CMake 검증은 `-DCMAKE_DISABLE_FIND_PACKAGE_Qt6=ON`, Qt 6 검증은
`-DCMAKE_DISABLE_FIND_PACKAGE_Qt5=ON`으로 반대 major 탐색을 막고 configure log와 link target을
확인한다. qmake는 `/usr/bin/qmake`/`/usr/bin/qmake6`와 `-query QT_VERSION`을 사용한다.
`CMAKE_PREFIX_PATH`만 주는 것은 Qt 5 강제가 아니다.

## 7. 실행과 검증

```bash
# 검증 (build/ici-* 는 ici 가 소유하는 shadow 트리. 이상하면 지우고 다시 돌린다)
cd loglens && QT_QPA_PLATFORM=offscreen ../../ici/dist/ici.pyz verify
cd diskmap && QT_QPA_PLATFORM=offscreen ../../ici/dist/ici.pyz verify

# 설치된 두 Qt major 확인
qmake -v       # Qt 5.15.18
qmake6 -v      # Qt 6.10.2
pkg-config --modversion Qt5Core Qt5Widgets Qt5Concurrent Qt5Test
pkg-config --modversion Qt6Core Qt6Widgets Qt6Concurrent Qt6Test

# 직접 빌드·테스트
cd loglens && cmake -S . -B build/check -DCMAKE_BUILD_TYPE=Debug && cmake --build build/check --parallel \
  && QT_QPA_PLATFORM=offscreen ctest --test-dir build/check --output-on-failure
cd diskmap && mkdir -p build/check && cd build/check && qmake6 ../../diskmap.pro && make -j"$(nproc)" \
  && QT_QPA_PLATFORM=offscreen make check TESTARGS=-xunitxml
```

GUI 는 Release 로 빌드해 두었다.

```bash
./loglens/build/release/src/gui/loglens-gui /tmp/live.log
./diskmap/build/release/src/gui/diskmap-gui ~/projects
```

`/tmp/feed-log.sh` 가 1초에 한 줄씩 `/tmp/live.log` 에 추가해 팔로우를 볼 수 있게 해준다.
`: > /tmp/live.log` 로 비우면 로테이션 처리(모델 리셋)가 보인다. **둘 다 임시 파일이라
재부팅하면 사라진다** — `loglens/tests/data/sample.log` 로 다시 만들면 된다.

## 8. 규약

`main` 직접 작업 금지, Conventional Commits, PR 로 병합. ici 의 `AGENTS.md` 와 같은 방식이다.
